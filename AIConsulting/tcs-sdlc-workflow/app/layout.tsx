import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TCS AI-Driven SDLC Transformation | L3 → L4 → L5",
  description: "Interactive visualization of AI-driven Software Development Lifecycle transformation across three maturity levels. From 26 weeks to 3.5 weeks with 87% time reduction.",
  keywords: ["AI SDLC", "Software Development", "AI Transformation", "DevOps", "Automation"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
